import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export default class LevelsList extends React.Component {
  render() {
    return (
       <Text>Levels List</Text>
    );
  }
}

const styles = StyleSheet.create({
  box: {
    flex: 1,
    flexDirection: 'row'
  }
});